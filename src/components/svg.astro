---
interface Props {
  id?: string;
  className?: string;
}

const { className = "size-5", id } = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 24 24"
  id={id}
  class={className}
>
  <slot />
</svg>
